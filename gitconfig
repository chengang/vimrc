# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = ChenGang
	email = chengang@win.local
[pull]
	rebase = true
	merge = true
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[http]
	postBuffer = 524288000
	version = HTTP/1.1
[pack]
	window = 1
[core]
	compression = 0
    packedGitLimit = 512m
    packedGitWindowSize = 512m
	excludesfile = /Users/gangchen/.gitignore_global
[pack]
    deltaCacheSize = 2047m
    packSizeLimit = 2047m
    windowMemory = 2047m

